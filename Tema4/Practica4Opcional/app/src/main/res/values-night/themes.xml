<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Practica4Opcional" parent="Theme.MaterialComponents">
        <!-- Customize your dark theme here. -->
        <item name="colorSecondary">@color/blu</item>
        <item name="contrastColor">@color/white</item>
        <item name="actionBarStyle">@style/AppTheme.ActionBar</item>
    </style>
    <!-- the theme applied to the application or activity -->

    <style name="AppTheme.ActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="background">@color/purp</item>
        <item name="android:height">150dp</item>
        <item name="height">60dp</item>
    </style>
</resources>