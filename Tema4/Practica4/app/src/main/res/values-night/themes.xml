<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Practica4" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorAccent">@color/red</item>
        <item name="colorPrimary">@color/blue</item>
        <item name="foregroundMainColor">@color/white</item>
        <item name="bg">@color/black</item>
        <item name="hintTextColor">@color/hintTextDark</item>
        <item name="hint">@color/hintTextLight</item>
        <item name="translucent">@color/translucentBlue</item>
    </style>
</resources>